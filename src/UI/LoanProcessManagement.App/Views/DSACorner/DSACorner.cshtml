

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "DSA Corner";
}

<style>
    .tile .tile-content {
        margin: 10px auto !important;
    }

    .tile .brand, .tile .tile-status {
        min-height: 30px !important;
    }
</style>
<div class="container">
</div>
<hr />
<div class="container report-list" style="margin-top: 40px;" runat="server" id="grid">
    <ul class="nav tiles nomargin">
        @foreach (var itm in Model)
        {
            <li class='col-lg-3 col-md-4 col-sm-6 col-xs-6'>
                <div class="tile bluediv bg-color-blueLight icon">
                    <a target="_blank" href="@itm.Link">
                        <div class="tile-content"><i><img src="@itm.Icon" /></i></div>
                        <div class="brand">@itm.DSAName</div>
                    </a>
                </div>
            </li>
        }
    </ul>
</div>
